{
  "name": "announcement-board",
  "version": "1.0.0",
  "description": "Traditional announcement board built with Express, EJS and Prisma.",
  "type": "module",
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch app.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "ejs": "^3.1.10",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "prisma": "^6.9.0"
  }
}
